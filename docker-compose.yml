version: "3.9"

services:
  storage-control-plane:
    build: .
    ports:
      - "127.0.0.1:8000:8000"
    env_file:
      - .env
    volumes:
      - ./config:/app/config
    restart: unless-stopped